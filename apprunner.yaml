version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: gunicorn --bind 0.0.0.0:8080 --workers 2 --timeout 60 wsgi:app
  network:
    port: 8080
    env: APP_PORT
  env:
    - name: FLASK_ENV
      value: production
